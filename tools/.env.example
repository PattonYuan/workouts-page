# Keep 自动同步凭据（本文件仅作模板，真实 .env 不要提交）
# 复制本文件为同目录 .env 并填入你的值：
#   cp tools/.env.example tools/.env
# .env 已被 .gitignore 忽略，不会进入仓库。

# Keep 登录手机号（用于非官方接口自动拉取）
KEEP_MOBILE=你的手机号

# Keep 登录密码
KEEP_PASSWORD=你的密码

# （可选）高驰凭据：填了之后，每日定时任务会同时自动拉取高驰（无需依赖 running_page 的 config.yaml）。
# 不填则定时任务只同步 Keep，高驰不会被自动拉取。
# 建议填上，这样 Coros + Keep 都全自动、零手动。
COROS_EMAIL=
COROS_PASSWORD=
