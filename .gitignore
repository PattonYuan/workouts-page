# 本地导出的运动文件（含个人数据，不要提交）
coros_activities/
keep_activities/
*.gpx
*.tcx
*.fit
__pycache__/
*.pyc
.DS_Store

# 凭据（含账号密码，绝不可提交）
.env
tools/.env

# 本机自动化日志（不进仓库，会无限增长）
logs/

# 增量同步游标状态（本机本地状态，不进仓库）
.keep_state.json
.coros_state.json

